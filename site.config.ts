type SiteConfig = {
  site_domain: string;
  site_name: string;
  site_description: string;
};

export const siteConfig: SiteConfig = {
  site_name: "Barber Shop",
  site_description: "Barber Shop",
  site_domain: "https://barber-shop.com",
};
